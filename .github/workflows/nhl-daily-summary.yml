name: NHL daily results to Telegram

on:
  schedule:
    - cron: "35 5 * * *"   # 06:35 Europe/London (как было)
  workflow_dispatch:
    inputs:
      local_date:
        description: "Локальная дата YYYY-MM-DD (например 2025-11-24)"
        required: false
        default: ""
      tz:
        description: "Таймзона IANA (например America/Los_Angeles)"
        required: false
        default: "Europe/Amsterdam"
      dry_run:
        description: "Только превью в логи (1) без отправки в TG"
        required: false
        default: "0"

permissions:
  contents: write  # не обязательно, но оставим как было (коммиты кэша)

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4

      - name: Run daily summary (explicit local day if provided)
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID:   ${{ secrets.TELEGRAM_CHAT_ID }}
          REPORT_DATE_LOCAL:  ${{ github.event.inputs.local_date }}
          REPORT_TZ:          ${{ github.event.inputs.tz }}
          DRY_RUN:            ${{ github.event.inputs.dry_run }}
        run: |
          python nhl_daily_results_bot.py
